# images

Image generation using `client.Images.Generate`.

## Run

```sh
XAI_API_KEY=... go run ./examples/images
```

## What it shows

- Submitting an `images.GenerateRequest` with model `aurora`
- Reading image URLs (or base64 JSON) from `ImageResponse.Data`
