# models

List all available language models with pricing.

## Run

```sh
XAI_API_KEY=... go run ./examples/models
```

## What it shows

- Calling `Models.ListLanguage` to get the full model catalog with pricing
- Iterating `LanguageModel` fields for model ID and token prices
